# Third cold review (of SYSTEM_PLAN.md revision 3) — 2026-09-18

Reviewer: a separate Fable agent with no prior context. Verified the second
review's eight changes are all IN. Call: REVISE, one pass, no further review
needed. Revision 4 makes these six changes.

## Structural findings all three missed

1. **No script drives the validate stage.** Only `validate.py freeze` and `verdict` were named; by elimination the interactive model sequenced the six children, did the redaction, looped on PIVOT, and nothing summed the $5 cap across passes (a PIVOT loop can cost $9–12). Change: `pipeline/validate.sh` as the only entry point, roles through `child.sh`, script-side redaction, stage-total cap, exit 3 at cap with no verdict.
2. **The judge has nothing to judge.** Ledger rows carry a hash and the fetcher's note, no body. Change: `fetch.py` stores extracted text under `bodies/<hash>.txt`; the judge reads files only, no Bash; bodies gitignored, verdict and ledger committed.
3. **Validation "before the interview" has no directory, and `jg-new-project.md` still runs its own feasibility check.** Change: `jg-new-project` creates the folder first and §1 becomes `/jg-validate`; a project-less NO-GO appends to the kit's `DEAD_ENDS.md`.
4. **`spec_check` cannot locate the verdict, and a default-on gate breaks every existing project, fixture and `build.sh` start.** Change: on GO write `spec.validation {slug, verdict_sha256, validated_budget_usd}`; `spec_check --gate-validate` at "signed" and in `build.sh`; specs without it are reported, not failed; exit 5 added to the pipeline table.
5. **The under-$20 path let the author set its own kill number.** Change: keep the setter; path budget $2.
6. **The over-$100 tier-3 gate had no enforcing script**, and `feedback.py` did not list the overrule re-check. Carried into the build: `milestone.py next` refuses; `feedback.py` re-checks.

## Build-order check

The first real run at $20 or more needs forum and search hosts the cloud blocks; the Mac was four steps later. Change: the first real run is under $20 in the cloud; the first $20+ run is on the Mac directly after. §4.0 stays first by choice (half a day, closes a principle violation) though it is not on the validation path; §4.3, §4.5 and most of §4.6 can follow the first run, except retiring the old feasibility paths, which must land first.

## Carried into the build

`milestone.py` tier-3 refusal; `feedback.py` overrule re-check; tier-1 independence requires a URL host or handle as `origin`, never free text; `hooks/guard.sh` needs the manifest path from `child.sh`.

## Ranked list

1. No script drove the validate stage; nothing enforced the cap across a PIVOT loop.
2. The judge saw no source body.
3. `spec_check` could not find the right verdict, and a default-on gate broke every existing caller.
4. The under-$20 path let the author set its own bar.
5. No directory for a pre-interview validation; `jg-new-project.md` kept a cheap feasibility path.
